<div class="container">
  <div class="row">
    <div class="col-xs-2 mt-30">
      <table class="table table-striped">
      	<thead>
      		<tr>
      			<th>ジャンル検索</th>
      		</tr>
      	</thead>
      	<tbody>
      		<% @genres.each do |genre|%>
      		<tr>
      			<td>
      			  <%= form_tag items_path, method: :get do %>
      			    <%= hidden_field_tag :id, genre.id %>
      			    <%= submit_tag genre.name %>
      			  <% end %>
      			</td>
      		</tr>
      		<% end %>
      	</tbody>
      </table>
    </div>
    <div class="col-xs-10">
      <div class="row">
        <div class="col-xs-12 ta-c">
          <p>ようこそ、NaganoCakeへ！</p>
          <p>このサイトは、ケーキ販売のECサイト</p>
          <p>会員でない方も商品の閲覧はできますが、</p>
          <p>購入には会員登録が必要なります。</p>
        </div>
      </div>
      <div class="row">
        <h1>おすすめ商品</h1>
        <% @items.each do |item|%>
          <div class="col-xs-3">
            <div>
              <%= link_to item_path(item) do %>
              <div>
                <%= attachment_image_tag item, :image, class:"picture" %>
              </div>
              <section>
                <%= item.name %>
              </section>
              <% end %>
              <section>
                ¥<%= (item.price * 1.1).round(0).to_s(:delimited) %>
              </section>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </div>

  <div class="row">
    <div class="col-xs-12">
      <%= link_to "もっと見る>", items_path, class:"float-right" %>
    </div>
  </div>
</div>